'use client';

import { Container, Typography } from '@mui/material';

export default function HomePage() {
  return (
    <Container>
      <Typography variant="h4">
        App is working 🚀
      </Typography>
    </Container>
  );
}